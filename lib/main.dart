import 'package:flutter/material.dart';
import 'screens/myswizzle.dart';

void main() => runApp(Swizzle());

class Swizzle extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MySwizzle();
  }
}
